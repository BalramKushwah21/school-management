export default function AdminFeesPage() {
  return (
    <main className="rounded-[28px] bg-white p-8 shadow-sm ring-1 ring-slate-200/70">
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-violet-700">
        Admin / Fees
      </p>
      <h1 className="mt-3 text-3xl font-bold text-slate-900">Fees</h1>
      <p className="mt-3 max-w-2xl text-slate-600">
        Fee schedules, payment history, and financial tracking are handled here.
      </p>
    </main>
  );
}